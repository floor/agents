#!/usr/bin/env bun
// Fixture: Codex output that never reaches the expected header — used to test that
// the adapter throws MalformedReviewError rather than returning unverifiable text.
console.log('Reading repository state...')
console.log('Something went wrong before a verdict could be formed.')
